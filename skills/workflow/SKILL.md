---
name: workflow
description: Basilic /w-* playbooks packed so install lands at .agents/skills/workflow/w-plan and the rest.
disable-model-invocation: true
---

Nested playbooks in this folder. Invoke `/w-plan`, `/w-build`, `/w-ship`, and the other `/w-*` names. Read the matching `w-<name>/SKILL.md`. Do not invent a parallel workflow tree.
